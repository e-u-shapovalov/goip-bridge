# Compiled binaries
/goip-bridge
*.exe
*.test
*.out

# Local runtime config and data (ship config.example.jsonc instead)
/config.json
*.db

# Runtime logs and the fallback journal (contain phone numbers and SMS text)
*.log
*.log.1
goip-bridge.fallback.jsonl

# Release artifacts (attached to GitHub Releases, not committed)
/goip-bridge-*.tar.gz
/checksums.txt
